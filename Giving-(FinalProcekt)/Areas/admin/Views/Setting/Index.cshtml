@model Setting
@{ ViewData["Title"] = "Index";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml"; }

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title text-center mb-4" style="font-size: 35px !important; font-weight: 600 !important; color: #3b579d !important;">Setting</h4>
                @{ if (Model == null)
                    {
        <a class="btn btn-primary mb-3 px-5" asp-area="Admin" asp-controller="SiteSetting" asp-action="Create">Create</a> }
    else
    {
<div class="table-responsive">
    <table class="table table-dark table-bordered table-striped">
        <thead>
            <tr style="text-align: center !important">
                <th style=" color: white;">
                    Logo
                </th>
                <th style=" color: white;">
                    Phone
                </th>
                <th style=" color: white;">
                    Email
                </th>
                <th style=" color: white;">
                    Address
                </th>
                <th style=" color: white;">
                    Iframe Src
                </th>
                <th style=" color: white;">
                    Update
                </th>
            </tr>
        </thead>
        <tbody>
            <tr class="text-center">
                <td class="py-1" style=" width: 100px !important; height: auto !important;">
                    <img style="border-radius: unset !important; width: 100% !important; height: 100% !important; object-fit: contain !important;" src="~/Uploads/@Model.Logo" alt="image" />
                </td>
                <td>
                    @Model.Phone
                </td>
                <td>
                    @Model.Email
                </td>
                <td>
                    @Model.Adress
                </td>
                <td>
                    @(Model.IframeLink != null && Model.IframeLink.Length > 20 ? Model.IframeLink.Substring(0, 20) + "..." : Model.IframeLink)
                </td>
                <td>
                    <a asp-controller="Setting" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-facebook" ><i class="fas fa-edit"></i></a>
                </td>
            </tr>
        </tbody>
    </table>
</div> } }

            </div>
        </div>
    </div>

</div>

